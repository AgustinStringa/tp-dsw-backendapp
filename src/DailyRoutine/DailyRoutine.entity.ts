// import {ObjectId} from "mongo"
export class DailyRoutine {
  constructor(
    date: Date,
    id_excercise: string, // it is ObjectId
    weight: number,
    series: number,
    repetitions: number
  ) {}
}
